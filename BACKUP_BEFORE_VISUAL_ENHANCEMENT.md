# Backup before visual enhancement

This marker was created before the visual enhancement pass.

The existing `main` branch remains unchanged while the enhancement is developed on the separate `visual-enhancement-work` branch. If the new design is not preferred, the existing main branch can be restored/used as the baseline.

Planned changes: remove all Gayathri font files/references, retain Anek Malayalam, refine application typography/layout, and redesign token/certificate print templates.