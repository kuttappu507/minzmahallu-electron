<?xml version="1.0" encoding="utf-8"?>
<!--
  Minz Mahallu Management System — native Android shell.

  There is no AndroidX / Gradle / Maven dependency here on purpose: the whole
  application is the web bundle in assets/, hosted by MainActivity's WebView.
  The shell only provides what a browser cannot: app-private file storage, the
  share sheet, device identity, preferences, the back button and the app
  lifecycle. See android/src/.../MainActivity.java.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mms.minzmahallu"
    android:versionCode="20000"
    android:versionName="2.0.0">

    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="34" />

    <!-- The app is fully offline. INTERNET is only used by the optional
         "message on WhatsApp" hand-off (wa.me click-to-chat). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Launch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|locale|density">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- mms://verify/<code> — what a scanned certificate/receipt QR
                 contains. Opens the app on the verification screen. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mms" android:host="verify" />
            </intent-filter>
        </activity>

        <!-- Hands generated files (PDF receipts, certificates, .mmbak backups,
             spreadsheet exports) to other apps as content:// URIs. -->
        <provider
            android:name=".ShareProvider"
            android:authorities="com.mms.minzmahallu.files"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
